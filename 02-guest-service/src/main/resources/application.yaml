server:
  port: 8081

spring:
  application:
    name: guest-service
  datasource:
    url: jdbc:mysql://localhost:3306/guest_db
    username: root
    password: yourpassword

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/